DATABASE_URL="postgresql://test:test@127.0.0.1:5434/rr_cesi_test"
AUTH_TOKEN_SECRET="test-secret"

# Desactive (presque) le rate limiting pour les tests e2e — les tests
# tapent les endpoints d'auth en boucle pour valider les scenarios.
THROTTLE_TTL=60000
THROTTLE_LIMIT=10000
THROTTLE_AUTH_LIMIT=10000
